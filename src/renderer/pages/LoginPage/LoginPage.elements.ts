import styled from 'styled-components';

export const Wrapper = styled.form`
	--gap: var(--gap-main);

	box-sizing: border-box;
	display: flex;
	flex-flow: column;
	gap: var(--gap);
	padding-bottom: 10px;
`;
