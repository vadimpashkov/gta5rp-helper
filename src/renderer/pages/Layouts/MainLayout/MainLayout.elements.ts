import styled from 'styled-components';

export const Wrapper = styled.div`
	z-index: var(--z-index-layout);
	margin-top: var(--height-drag-panel);
`;

export const Navigation = styled.nav`
	display: flex;
	flex-flow: column;
`;
